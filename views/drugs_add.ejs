<!DOCTYPE html>
<html lang="en">
<head>
  <title>Add New Drug - Pharmacy</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <link href="https://fonts.googleapis.com/css?family=Inter:wght@500;700&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      background: linear-gradient(120deg, #f3fafd 0%, #e3ecfa 100%);
      font-family: 'Inter', Arial, sans-serif;
    }
    .container {
      max-width: 480px;
      margin: 42px auto;
      background: #fff;
      border-radius: 14px;
      box-shadow: 0 12px 38px #2179e71a, 0 2px 5px #eaf6fe;
      padding: 32px 32px 22px 32px;
    }
    h2 {
      color: #2174c4;
      text-align: center;
      margin-bottom: 24px;
      font-weight: 700;
      font-size: 1.7rem;
      letter-spacing:0.01em;
      display:flex; align-items:center; justify-content:center; gap:9px;
    }
    form label {
      display: block;
      margin: 11px 0 4px 3px;
      font-weight: 600;
      letter-spacing:0.009em;
      color: #21487b;
      font-size: 1.07em;
    }
    form input, form select {
      width: 100%;
      padding: 9px 10px; border:1.2px solid #bcd2e6;
      border-radius: 7px;
      font-size: 1rem;
      margin-bottom: 8px;
      background: #f8fbfd;
      transition: border 0.16s, box-shadow 0.17s;
    }
    form input:focus, form select:focus {
      border:1.7px solid #239cb8;
      outline: none;
      box-shadow: 0 2px 12px #2ac1e013;
    }
    small {
      color:#919dae; font-size:0.96em;
      display:block; margin-bottom:7px; margin-top:-6px;
    }
    .presc-row {display:flex;align-items:center;gap:7px;margin-top:7px;font-size:1.04em;}
    .req-badge {
      display:inline-block; margin-left:6px;
      color: #228801; background: #e8f9e3;
      font-weight:700; font-size:0.99em; border-radius:9px 5px;
      padding:2.5px 10px;
    }
    .btn {
      width: 100%;
      background: linear-gradient(90deg, #239cb8 60%, #2174c4 99%);
      color: #fff;
      border: none;
      padding: 12px 0;
      font-size: 1.1em;
      margin-top: 15px;
      border-radius: 7px;
      cursor: pointer;
      font-weight: 700;
      letter-spacing: 0.03em;
      box-shadow: 0 2px 10px #2174c433;
      transition: background 0.18s;
    }
    .btn:hover, .btn:focus { background:linear-gradient(90deg,#1882af 60%,#174e9a 99%);}
    .success {
      color: #1c7f14;
      background: #e4f6e8;
      padding: 9px 0;
      border-radius: 5px;
      margin-bottom: 9px;
      text-align: center;
      font-weight: 600;
      font-size: 1.04em;
    }
    .error {
      color: #c02c1d;
      background: #fcecec;
      padding: 8px 0;
      border-radius: 5px;
      margin-bottom: 9px;
      text-align:center;
      font-weight:600;font-size: 1.03em;
    }
    .links {
      margin-top: 18px;
      text-align: center;
    }
    .links a {
      color: #2174c4;
      text-decoration: underline;
      font-weight: 500;
      font-size:1.01em;
    }
    .links a:hover {color:#239cb8;}
    @media (max-width: 600px) {
      .container { padding: 2vw 1vw;}
      h2{font-size:1.22rem;}
      form label, input, select {font-size:1em;}
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>
      <svg width="28" height="28" viewBox="0 0 40 40" fill="none">
        <circle cx="20" cy="20" r="20" fill="#e2f7ee"/>
        <rect x="11" y="16" width="18" height="8" rx="4" fill="#27b8a0"/>
        <rect x="17.1" y="10.7" width="6" height="18.6" rx="3" transform="rotate(45 20 20)" fill="#71cbfa"/>
      </svg>
      Add New Drug
    </h2>
    <% if (typeof message !== "undefined" && message) { %>
      <div class="<%= message.success ? 'success' : 'error' %>"><%= message.text %></div>
    <% } %>

    <form method="POST" action="/drugs/add" autocomplete="off">
      <label for="code">Drug Code <span style="color:#e61419;font-weight:700;">*</span></label>
      <input id="code" name="code" required maxlength="25" placeholder="Ex: PCM500">

      <label for="name">Drug Name <span style="color:#e61419;font-weight:700;">*</span></label>
      <input id="name" name="name" required maxlength="80" placeholder="Ex: Paracetamol">

      <label for="generic_name">Generic Name</label>
      <input id="generic_name" name="generic_name" maxlength="80" placeholder="Ex: Acetaminophen">

      <label for="manufacturer">Manufacturer</label>
      <input id="manufacturer" name="manufacturer" maxlength="50" placeholder="Ex: Cipla Ltd">

      <label for="classification">Classification</label>
      <input id="classification" name="classification" maxlength="40" placeholder="Ex: Analgesic">

      <div class="presc-row">
        <input type="checkbox" name="required" id="required" value="1" style="width:auto;display:inline-block;">
        <label for="required" style="margin:0;display:inline;color:#137187;cursor:pointer;">Prescription Required
          <span class="req-badge">Rx</span>
        </label>
      </div>
      <small>Tick if this drug is sold only with prescription.</small>

      <button type="submit" class="btn">Save Drug</button>
    </form>

    <div class="links">
      <a href="/drugs">← Back to Drug Master</a>
    </div>
  </div>
</body>
</html>
