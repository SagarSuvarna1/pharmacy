<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pharmacy Login</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <link href="https://fonts.googleapis.com/css?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      min-height: 100vh;
      margin: 0;
      background: radial-gradient(circle at 50% 0%, #d1f3ff 0%, #a4f9ef 40%, #c4e2fe 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Inter', Arial, sans-serif;
    }
    .login-card {
      width: 100%;
      max-width: 370px;
      background: #fff;
      border-radius: 20px;
      box-shadow: 0 10px 38px 0 rgba(32, 168, 146, 0.18), 0 1.5px 8px 0 #67c5ecd9;
      padding: 38px 32px 32px 32px;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 16px;
      transition: box-shadow 0.2s;
    }
    .login-logo {
      width: 58px;
      height: 58px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 8px;
      border-radius: 50%;
      background: linear-gradient(90deg, #e1f3ee, #c4e2fe 60%, #aee1ec 100%);
      box-shadow: 0 3px 14px #a4f9ef4a;
      position: relative;
    }
    .login-logo svg {
      width: 34px;
      height: 34px;
      display: block;
    }
    h2 {
      color: #25a18e;
      text-align: center;
      margin: 0 0 10px 0;
      font-size: 1.7rem;
      font-weight: 700;
      letter-spacing: 0.01em;
    }
    form {
      width: 100%;
      display: flex;
      flex-direction: column;
      gap: 15px;
      margin-bottom: 5px;
    }
    form label {
      display: block;
      font-weight: 600;
      color: #388d7a;
      letter-spacing: 0.01em;
      margin-bottom: 6px;
      margin-top: 2px;
    }
    form input[type="text"], form input[type="password"] {
      width: 100%;
      height: 41px;
      padding: 10px 12px;
      border: 1.7px solid #67c5ec;
      border-radius: 9px;
      background: #f7fbfd;
      font-size: 1.04rem;
      color: #286e65;
      transition: border 0.2s, box-shadow 0.2s;
    }
    form input:focus {
      border: 1.7px solid #25a18e;
      outline: none;
      box-shadow: 0 3px 11px #25a18e18;
      background: #f1fbfa;
    }
    form input::placeholder {
      color: #b2c9d9;
      font-size: 1.04rem;
      opacity: 1;
      letter-spacing: 0.01em;
      font-family: inherit;
    }
    .btn {
      width: 100%;
      background: linear-gradient(90deg, #27b8a0 60%, #5acbf7 100%);
      color: #fff;
      border: none;
      padding: 13px 0 12px 0;
      font-size: 1.09rem;
      margin-top: 6px;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 700;
      letter-spacing: 0.03em;
      box-shadow: 0 3px 12px #25a18e17;
      transition: background 0.2s, box-shadow 0.2s;
    }
    .btn:hover {
      background: linear-gradient(90deg, #21ae92 40%, #28bdee 100%);
      box-shadow: 0 6px 22px #25a18e22;
    }
    .error {
      width: 100%;
      color: #c0392b;
      background: #fdeeee;
      padding: 9px 0 8px 0;
      margin-bottom: 7px;
      border-radius: 6px;
      text-align: center;
      font-weight: 600;
      font-size: 1.02rem;
      letter-spacing: 0.01em;
    }
    .footer {
      width: 100%;
      font-size: 13px;
      color: #9fc3b4;
      text-align: center;
      margin-top: 18px;
      letter-spacing: 0.03em;
      padding-bottom: 2px;
    }
    @media (max-width: 480px) {
      .login-card {
        padding: 22px 6vw 24px 6vw;
      }
      .btn { font-size: 1rem; }
    }
  </style>
</head>
<body>
  <div class="login-card">
    <div class="login-logo">
      <!-- Stylish pharmacy cross icon (SVG) -->
      <svg fill="none" viewBox="0 0 40 40">
        <circle cx="20" cy="20" r="20" fill="#e1f3ee"/>
        <rect x="14" y="18" width="12" height="4" rx="2" fill="#27b8a0"/>
        <rect x="18" y="14" width="4" height="12" rx="2" fill="#71cbfa"/>
      </svg>
    </div>
    <h2>Pharmacy Login</h2>
    <!-- Error display block, for server-side (Node/EJS) integration -->
    <% if (typeof error !== "undefined" && error) { %>
      <div class="error"><%= error %></div>
    <% } %>
    <form method="post" action="/login" autocomplete="off">
      <label for="username">Username</label>
      <input id="username" name="username" type="text" required autofocus autocomplete="username" placeholder="Enter your username">

      <label for="password">Password</label>
      <input id="password" name="password" type="password" required autocomplete="current-password" placeholder="Enter your password">

      <button class="btn" type="submit">Login</button>
    </form>
    <div class="footer">
      &copy; <span id="cy"></span> My Pharmacy
    </div>
  </div>
  <script>
    // Set copyright year
    document.getElementById("cy").textContent = new Date().getFullYear();
  </script>
</body>
</html>
