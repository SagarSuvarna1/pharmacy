<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pharmacy Login</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <link href="https://fonts.googleapis.com/css?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      min-height: 100vh;
      margin: 0;
      background: linear-gradient(120deg, #e1f3ee 0%, #c4e2fe 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Inter', Arial, sans-serif;
    }
    .login-card {
      width: 100%;
      max-width: 350px;
      background: #fff;
      border-radius: 14px;
      box-shadow: 0 8px 32px rgba(32,168,146,0.16);
      padding: 36px 28px 28px 28px;
      display: flex;
      flex-direction: column;
      gap: 16px;
      transition: box-shadow 0.2s;
    }
    .login-card:hover {
      box-shadow: 0 12px 36px rgba(32,168,146,0.23);
    }
    .login-logo {
      display: flex;
      justify-content: center;
      align-items: center;
      margin-bottom: 10px;
    }
    .login-logo svg {
      width: 48px;
      height: 48px;
    }
    h2 {
      color: #25a18e;
      text-align: center;
      margin: 0 0 4px 0;
      font-size: 1.6rem;
      letter-spacing: 0.02em;
      font-weight: 600;
    }
    form label {
      display: block;
      margin: 12px 0 6px 2px;
      font-weight: 500;
      color: #3f5866;
      letter-spacing: 0.02em;
    }
    form input[type="text"], form input[type="password"] {
      width: 100%;
      padding: 10px 12px;
      border: 1px solid #78ded7;
      border-radius: 8px;
      margin-bottom: 6px;
      font-size: 1rem;
      transition: border 0.2s, box-shadow 0.2s;
      background: #f8fafb;
    }
    form input:focus {
      border-color: #25a18e;
      outline: none;
      box-shadow: 0 2px 8px #25a18e25;
    }
    .btn {
      width: 100%;
      background: linear-gradient(90deg, #27b8a0 60%, #71cbfa 100%);
      color: #fff;
      border: none;
      padding: 12px 0;
      font-size: 1.11rem;
      margin-top: 12px;
      border-radius: 7px;
      cursor: pointer;
      font-weight: 600;
      letter-spacing: 0.03em;
      box-shadow: 0 2px 12px #25a18e22;
      transition: background 0.2s;
    }
    .btn:hover {
      background: linear-gradient(90deg, #21ae92 40%, #53b2e7 100%);
    }
    .error {
      color: #c0392b;
      background: #fdeeee;
      padding: 10px 0;
      margin-bottom: 6px;
      border-radius: 5px;
      text-align: center;
      font-weight: 600;
      font-size: 0.97rem;
    }
    .footer {
      font-size: 13px;
      color: #9fc3b4;
      text-align: center;
      margin-top: 18px;
      letter-spacing: 0.03em;
    }
    @media (max-width: 480px) {
      .login-card {
        padding: 22px 6vw 24px 6vw;
      }
      .btn { font-size: 1rem; }
    }
  </style>
</head>
<body>
  <div class="login-card">
    <div class="login-logo">
      <!-- Simple pharmacy icon (SVG) -->
      <svg viewBox="0 0 40 40" fill="none">
        <g>
          <circle cx="20" cy="20" r="20" fill="#e1f3ee"/>
          <rect x="11" y="16" width="18" height="8" rx="4" fill="#27b8a0"/>
          <rect x="17.1" y="10.7" width="6" height="18.6" rx="3" transform="rotate(45 20 20)" fill="#71cbfa"/>
        </g>
      </svg>
    </div>
    <h2>Pharmacy Login</h2>
    <!-- Replace server-side error syntax with your backend's error system -->
    <!-- Example: <div class="error">Invalid username or password</div> -->
    <form method="post" action="/login" autocomplete="off">
      <label for="username">Username</label>
      <input id="username" name="username" required autofocus autocomplete="username" placeholder="Enter your username">

      <label for="password">Password</label>
      <input id="password" name="password" type="password" required autocomplete="current-password" placeholder="Enter your password">

      <button class="btn" type="submit">Login</button>
    </form>
    <div class="footer">
      &copy; <span id="cy"></span> My Pharmacy
    </div>
  </div>
  <script>
    // Set copyright year
    document.getElementById("cy").textContent = new Date().getFullYear();
  </script>
</body>
</html>
